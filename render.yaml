services:
  - type: web
    name: sakhi-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: GOOGLE_API_KEY
        value: your-api-key-here
